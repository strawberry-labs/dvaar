# Server Configuration
HOST=0.0.0.0
PORT=8080
INTERNAL_PORT=6000

# Domains
BASE_DOMAIN=dvaar.io          # Main brand domain (api, docs, admin, dash)
TUNNEL_DOMAIN=dvaar.app       # User tunnel URLs (*.dvaar.app)
PUBLIC_URL=https://api.dvaar.io

# Node Identity (for distributed routing)
NODE_IP=127.0.0.1
CLUSTER_SECRET=your-cluster-secret-here

# Database
DATABASE_URL=postgres://postgres:password@localhost:5432/dvaar

# Redis
REDIS_URL=redis://localhost:6379

# GitHub OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Stripe (for billing)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Logging
RUST_LOG=info,dvaar_server=debug,dvaar_cli=debug
